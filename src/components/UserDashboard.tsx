import React from 'react';

const UserDashboard = () => {
  return (
    <div>
      <h1>User Dashboard</h1>
      {/* Add user information and links to recipes, map, and profile here */}
    </div>
  );
};

export default UserDashboard;
